import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  defaultContainerStyle: {
    overflow: 'hidden',
  },
  defaultButtonStyle: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
