import {Theme} from '@atoms';
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  background: {
    position: 'absolute',
    top: 0,
    left: 0,
  },
  voucherLogo: {
    backgroundColor: Theme.Yellow02,
    borderRadius: 12,
  },
});

export default styles;
